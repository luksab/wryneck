egg hatch() {
    // does this work?
    let x = {
        let test = 420; // testing
    };

    let str = "hello";
    "hello";

    if true {
        if false {
            let x = 420;
        } else {
            let x = "hello";
        };
        "world";
    };

    let y = count_Pigeons(test);
    *)> (69 + {*)>30 + 1;}) * 3;
}
// does things
🥚 count_Pigeons(pigeons) {
    *)> 1;
    // and stuffs
}
[
    2 = 0,
    4 = 0,
]
